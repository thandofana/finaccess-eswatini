# Optional comma-separated origins for a separately hosted/local Phase 10 API.
# The production Vercel application is same-origin and does not require CORS.
FINACCESS_CORS_ORIGINS=https://your-frontend.example
